#!/bin/bash
# Double-click in Finder to launch the Job Application GUI App
# This opens a new Terminal session automatically and runs the app.

# Navigate to the directory containing this script
cd "$(dirname "$0")"

# Execute the main launcher script
./launch.sh
